# shir_nodes/array_len.node — primitive: length of an array.
# Every language: .length / .len() / count(). Composes with Split: wc -l = ArrayLen(Split(t,"\n")).

node ArrayLen
tag "ArrayLen"
kind expr
# storage_class str_bounded
field array: expr
