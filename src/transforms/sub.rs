// Placeholder transform module — the estree worker's compile-in replaces
// this with real worker-submitted transform files.
pub fn transform(_stmts: &mut Vec<crate::ir::IrStmt>) -> bool {
    false
}
